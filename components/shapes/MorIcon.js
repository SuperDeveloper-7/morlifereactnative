import { createIconSetFromFontello } from '@expo/vector-icons';
import morIConConfig from '../../assets/fonts/mor-icons-config.json';

const Icon = createIconSetFromFontello(morIConConfig, 'mor-icons', 'mor-icons.ttf');

export default Icon;
